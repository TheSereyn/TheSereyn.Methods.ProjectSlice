# Search

Current implemented state:

- S-002 allows a user to search persisted document titles using partial and case-insensitive matching.
- Search returns document references that can be opened through the existing document flow.
- Full-text search, fuzzy matching, and filters remain future work.