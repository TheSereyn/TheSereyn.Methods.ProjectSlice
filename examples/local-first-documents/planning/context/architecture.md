# Architecture Context

The initial system is deliberately local-first. Persistence, search, and export should be shaped so that later synchronization can be added without reworking the first milestone.