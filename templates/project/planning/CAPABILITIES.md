# Capabilities

Use this file when a separate capability map adds clarity.

- `C-001` Core workflow
- `C-002` Supporting workflow

Replace the placeholders with stable capability IDs and names that describe the shape of the finished project, not the delivery order.