---
agent: agent
description: "Project Slice Method implementation. Use this to implement the current slice when it is already ready or active, while preserving scope boundaries and validation discipline."
---

Use the `psm-project-manager` agent for this request.

If the active slice is already `ready` or `active`, route implementation to the correct internal specialist.

If no slice is ready, do not start coding. Route back to slice planning and explain what still blocks implementation.

Keep unrelated ideas out of the current implementation scope.