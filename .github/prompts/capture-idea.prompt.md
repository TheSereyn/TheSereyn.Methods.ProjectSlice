---
agent: agent
description: "Project Slice Method tangent capture. Use this to save a useful idea for later without expanding the current slice or roadmap scope."
---

Use the `psm-project-manager` agent for this request.

Capture a useful tangent in the active plan root `INBOX.md` without changing the current slice boundary.

If the idea text is missing, ask for the smallest possible follow-up.

After capturing the idea, return to the current workflow instead of elaborating the idea into requirements or roadmap work.